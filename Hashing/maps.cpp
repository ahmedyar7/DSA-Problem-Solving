#include <bits/stdc++.h>
using namespace std;

map<int, int> map_insert(int arr[], int n)
{
    map<int, int> mp;
    for (int i = 0; i < n; i++)
    {
        map[arr[i]] = i;
    }
    return mp;
}

int main()
{
}